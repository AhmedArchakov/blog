/* Модальное окно*/

/*let modal = document.querySelector('.search-overlay');
let btn = document.querySelector('.search-toggle');

btn.addEventListener('click', openModal);

function openModal() {
    modal.classList.add('active');
    attachEvents();
}

function closeModal() {
    modal.classList.remove('active');
    detachEvents();
}

function attachEvents() {
    document.querySelector('.search-overlay-close').addEventListener('click', closeModal);
    document.addEventListener('keydown', closeEscape);
}

function detachEvents() {
    document.querySelector('.search-overlay-close').removeEventListener('click', closeModal);
    document.removeEventListener('keydown', closeEscape);
}

function closeEscape(event) {
    if (event.code === 'Escape') {
        closeModal();
    }
}*/

/* END Модальное окно*/
